node_modules
dist
*.local
.DS_Store
tsconfig.tsbuildinfo

# Local assistant context pack — contains client and payroll detail,
# kept on the local machine rather than pushed to the remote.
assistant-context/

# Seed data holds real client and payroll figures — kept out of the repo.
# Generate it with: npm run seed -- public/seed.json <mastersheet.xlsx>
public/seed.json
seed.json
e2e-out/
# The books themselves — real client figures. Generated, never committed:
#   node scripts/seed-sql.mjs <seed.json> supabase/seed-periods.sql
supabase/seed-periods.sql
